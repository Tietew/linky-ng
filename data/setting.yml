--- 
persist:
  value: boolean
  usage: "SET PERSIST <on / off>"

default_wiki:
  value: text
  usage: "SET DEFAULT_WIKI <wiki>"

default_lang:
  value: text
  usage: "SET DEFAULT_LANG <lang>"

nowiki:
  value: boolean
  usage: "SET NOWIKI <on / off>"

statusreport:
  value: boolean
  usage: "SET STATUSREPORT <on / off>"
